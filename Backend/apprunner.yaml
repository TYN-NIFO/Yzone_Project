version: 1.0
runtime: nodejs22
build:
  commands:
    build:
      - npm install
      - npm run build
run:
  command: node dist/server.js
  network:
    port: 5000
    env: PORT
  env:
    - name: NODE_ENV
      value: production
